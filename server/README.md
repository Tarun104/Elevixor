Backend server for NextHire

Quick start:

1. Copy `.env.example` to `.env` and fill values.
2. Install dependencies: `npm install`.
3. Start in development: `npm run dev`.

APIs mounted under `/api/*` (auth, contact, quote, newsletter, dashboard, placeholder).
